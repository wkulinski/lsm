# GitHub CLI and API authentication (optional for local development).
GH_TOKEN=

# Optional tool wrapper prefix. Leave empty to use repository-native commands.
BIN_PATH=""

# Local agent cache and state directory.
CACHE_PATH="var/agent/cache/"

# Standards profile switch used by shared agent skills.
CQRS_MONOLITH_STANDARD_OVERRIDES="0"
